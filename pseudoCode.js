// prettier-ignore
// Function to change background on landing page.
// If user clicked on background toggle buttons - Start
  //If background already showing
    // Yes, continue execution.
    // No, set New background to Previous background.
  // Reset Previous background
  //Get number from clicked event handler.
    //If number is 1
      //If New background is same as Previous
          // Yes, don't reset background
          // No, reset saved background to background type 1
          // break execution
    //If number is 2
      //If New background is same as Previous
          // Yes, don't reset background
          // No, reset saved background to background type 2
          // break execution
    //If number is 3
      //If New background is same as Previous
          // Yes, don't reset background
          // No, reset saved background to background type 3
          // break execution